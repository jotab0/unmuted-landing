{
  "name": "unmuted-landing",
  "version": "2.0.0",
  "description": "Unmuted — AI translation that preserves your personality",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static site — no build step required'"
  }
}
